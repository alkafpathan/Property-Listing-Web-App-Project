<style>
  
</style>

<nav class="navbar navbar-expand-md sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand text-danger fw-bold" href="/listings">
      <i class="fa-regular fa-compass me-2"></i>EasyList
    </a>

    <!-- Hamburger button -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible content -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- Search form -->
      <form class="search-form mx-auto" role="search">
        <input
          class="search-input"
          type="search"
          placeholder="Search destinations"
          aria-label="Search"
        />
        <button class="search-btn" type="submit">
          <i class="fa-solid fa-magnifying-glass"></i>
          Search
        </button>
      </form>

      <!-- Auth links -->
      <div class="navbar-nav ms-auto text-center mt-3 mt-md-0">
        <a class="nav-link fw-semibold" href="/listings/new">EasyList your home</a>
        <% if (!currUser) { %>
          <a class="nav-link fw-bold" href="/signup">Sign up</a>
          <a class="nav-link fw-bold" href="/login">Log in</a>
        <% } %>
        <% if (currUser) { %>
          <a class="nav-link fw-bold text-danger" href="/logout">Log out</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
